# Code Generated by Sidekick is for learning and experimentation purposes only.
} catch {
    $resp = $_.Exception.Response
    $status = $resp.StatusCode.value__
    $wwwAuth = $resp.Headers['WWW-Authenticate']
    Write-Host "HTTP $status" -ForegroundColor Red
    if ($wwwAuth) { Write-Host "WWW-Authenticate: $wwwAuth" -ForegroundColor Yellow }
    if ($_.ErrorDetails.Message) { Write-Host $_.ErrorDetails.Message -ForegroundColor Yellow }
}
